- hosts: local
  gather_facts: true
  become: false   # <— do not elevate during facts

  tasks:
    - name: creating directory
      file:
        path:"/temp/jenkinsdirectory_test"
        state:directory
        mode:"0775"
